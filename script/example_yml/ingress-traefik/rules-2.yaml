apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dl-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: synchronizers
spec:
  rules:
    - host: ec2-52-4-20-43.compute-1.amazonaws.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: synchronizers-v2
                port:
                  number: 5555
